# Shadow Execution Map

| surface | repo path | status | notes |
| --- | --- | --- | --- |
| Forbidden transition bypass on governed operator write path | `app/hq/governance/transition_gate.py::validate_transition`; `signal_agent/operator/runtime.py::_validate_write_transition` | blocked | Existing tests prove the gate denies forbidden and undefined transitions before handler execution. |
| Post-dispatch mismatch handling | `signal_agent/operator/runtime.py::_dispatch_tool`; `signal_agent/operator/runtime.py::_consistency_requires_rejection`; `signal_agent/operator/runtime.py::_classify_transactional_consistency` | blocked on declared operator surfaces | Mismatch is still detected after handler execution, but it now halts the run with `contract_violation` on declared operator surfaces. |
| Undeclared-write blind spot when `lifecycle_writes` is empty | `signal_agent/operator/runtime.py::_dispatch_tool`; `signal_agent/operator/runtime.py::_tool_observation_scope_paths` | partially blocked | Zero-write tools now reject observed mutation on explicit observation scopes. Mutation outside that bounded scope remains unproven. |
| Direct plan execution bypasses parser entry | `signal_agent/operator/runtime.py::execute`; `signal_agent/operator/planner.py::OperatorPlan` | partially blocked | Parser entry is still not exclusive, but ready runtime entrypoints are now registry-bound rather than unconstrained alternate authority surfaces. |
| Routing authority fallback to `member_inference` | `shared/contract.py::resolve_bundle_contract`; `app/hq/capture/router.py::route_bundle` | blocked | `member_inference` is now non-authoritative and router dispatch fails closed when it is the only contract source. |
| Registry lookup exception fall-through in contract resolution | `shared/contract.py::resolve_bundle_contract` | partially blocked | Broad `except Exception: pass` hides registry lookup failures and falls through to weaker sources. |
| Router config fallback and plain log append | `app/hq/capture/router.py::_load_spine_config`; `app/hq/capture/router.py::_append_routing_log`; `app/hq/capture/router.py::route_bundle` | partially blocked | Missing or unreadable config falls back to `misc`; routing log uses plain append. |
| Curate direct staged-file writes and hardcoded paths | `app/hq/curation/curate.py::register_artifact`; `app/hq/curation/curate.py::update_index`; `app/hq/curation/curate.py::curate_file` | partially blocked | Transition gate and governor run before the main copy path, and registry/index persistence now use the shared IO contract, but the staged file copy and hardcoded paths remain outside operator-style boundary verification. |
| Release manifest and registry persistence | `services/release_orchestrator/runner.py::run_release`; `services/release_orchestrator/feedback.py::save_constraints` | partially blocked | Local FSM exists, and manifest/constraints/registry persistence now use the shared IO contract, but release still lacks operator-style declared-vs-observed boundary verification. |
| Intake direct ledger and summary writes | `app/intake/intake.py` | partially blocked | Intake uses transition events, but ledger and summary writes still use direct `open()` calls. |
| Promote direct bundle and log writes | `app/hq/capture/promote.py` | partially blocked | Promotion uses transition checks and state registry, but still writes bundle files and logs directly. |
