# Implementation Evidence

| concept | exact enforcement files | exact proving tests | enforcement status | claim label | notes |
|---|---|---|---|---|---|
| declared operator intent | `signal_agent/operator/intent.py`; `signal_agent/operator/registry.py`; `signal_agent/operator/runtime.py`; `config/operator/intents.yaml` | `tests/test_operator_write_intent_contract.py`; `tests/test_operator_parser_entry.py` | enforced on operator ready-plan entrypoints | `repo-proven` | Write intents are parsed narrowly, and ready runtime entrypoints reject plan/intent/workflow mismatches against the bounded registry. |
| declared operator read/write boundary | `config/operator/tools.yaml`; `config/operator/workflows.yaml`; `signal_agent/operator/runtime.py` | `tests/test_operator_write_contract.py` | enforced on operator write path | `repo-proven` | Tools declare reads/writes and workflows declare authority and write paths. |
| mutating tools blocked in non-write workflows | `signal_agent/operator/runtime.py`; `config/operator/tools.yaml`; `config/operator/workflows.yaml` | `tests/test_operator_write_contract.py` | enforced on operator write path | `repo-proven` | Runtime rejects dispatch of mutating tools outside write-mode workflows. |
| transition validation before write dispatch | `app/hq/governance/transition_gate.py`; `signal_agent/operator/runtime.py`; `config/state_machine.yaml`; `config/lanes.yaml` | `tests/test_operator_write_denial.py`; `tests/security/test_transition_bypass.py` | enforced on governed transition paths | `repo-proven` | Unknown states, forbidden transitions, undefined transitions, and policy failures are denied before the handler runs. |
| duplicate read-before-write prevention | `signal_agent/operator/runtime.py`; `config/operator/workflows.yaml` | `tests/test_operator_duplicate_gate.py`; `tests/test_operator_compound_parser_entry.py` | enforced on operator append path | `repo-proven` | Duplicate intake appends are rejected before mutation. |
| non-transactional boundary evidence and rejection | `signal_agent/operator/runtime.py` | `tests/test_operator_write_contract.py`; `tests/test_operator_write_denial.py` | enforced on declared operator write surfaces | `repo-proven` | Runtime records observed changes and rejects `no_effect_observed` instead of returning `ok`. |
| transactional snapshot and hash evidence | `signal_agent/operator/runtime.py`; `config/operator/tools.yaml` | `tests/test_operator_transaction_snapshot.py` | enforced on operator transactional path | `repo-proven` | Transactional overwrite paths capture pre-state snapshots and hash-based post-state evidence. |
| observation-scope mutation rejection for zero-write and declared-write tools | `signal_agent/operator/runtime.py`; `config/operator/tools.yaml`; `config/operator/workflows.yaml` | `tests/test_operator_write_contract.py`; `tests/test_operator_write_denial.py` | enforced on explicit operator observation scopes | `repo-proven` | Zero-write tools reject observed mutation on declared reads, authority paths, workflow write surfaces, and immediate non-root parent directories of declared file surfaces. Declared-write tools reject in-scope writes outside their declared write set, while the exact `.lock` companion of a declared append target is treated as auxiliary rather than rogue mutation. |
| operator run ledger atomic append | `signal_agent/operator/runtime.py`; `app/utils/io_contract.py` | `tests/test_operator_write_contract.py` | enforced on operator ledger path | `repo-proven` | `operator_runs.jsonl` now appends through `append_jsonl_atomic`. |
| append-only artifact-state registry | `shared/state_registry.py`; `app/utils/io_contract.py` | no dedicated test cited in this bundle | enforced on named registry path | `repo-supported` | The code path is direct and append-only, but this bundle does not rely on a dedicated registry test. |
| governor fail-closed blocking | `app/governor/activation_governor.py`; `app/utils/io_contract.py` | `tests/test_activation_governor.py` | enforced on governor-covered scopes | `repo-proven` | Drift, invalid state, and unauthorized scope can block mutating operations. |
| routing contract resolution with stale guard | `shared/contract.py`; `app/hq/capture/router.py` | `tests/test_phase2_improvements.py` | enforced | `repo-proven` | Registry hits are ignored when the bundle file is missing, and unresolvable contracts fail. |
| inference-only routing rejection | `shared/contract.py`; `app/hq/capture/router.py` | `tests/test_phase2_improvements.py` | enforced on routing eligibility decision | `repo-proven` | `member_inference` remains a low-confidence signal, but it is non-authoritative and router dispatch fails closed when no registry or frontmatter contract exists. |
| routing ledger emission | `app/hq/capture/router.py` | `tests/test_phase2_improvements.py` | enforced on named routing log path | `repo-proven` | Routing writes structured log entries for success and failure states. |
| release-local lifecycle FSM | `services/release_orchestrator/contract.py` | `tests/test_release_orchestrator.py` | enforced inside release orchestrator | `repo-proven` | The release-local FSM permits only `draft -> extracted -> adapted -> checked -> emitted`. |
| release manifest, constraints, and registry emission | `services/release_orchestrator/runner.py`; `services/release_orchestrator/feedback.py`; `app/utils/io_contract.py` | `tests/test_release_orchestrator.py`; `tests/test_publication_pipeline_end_to_end.py` | enforced on named release persistence surfaces | `repo-proven` | Tests now cover manifest emission, constraints persistence, registry append, helper usage, append-failure cleanup, and end-to-end release emission from a staged publication artifact. The release path still does not prove operator-style observed-vs-declared mutation verification. |
| curated publication staging surface | `app/hq/curation/curate.py`; `app/hq/governance/transition_gate.py`; `config/policies/publication_policy.yaml`; `app/utils/io_contract.py` | `tests/test_curate_publication_gate.py`; `tests/test_source_video_registration.py`; `tests/test_publication_pipeline_end_to_end.py` | enforced on named staged surface | `repo-proven` | `app/hq/curation/curate.py` now has focused tests proving `compiled -> staged` rejection, successful staged output creation, atomic registry append, atomic index rewrite, and one real downstream handoff into the release runner. |
| one named publication path from compiled docs input to staged artifact to emitted release state | `app/hq/curation/curate.py`; `app/hq/governance/transition_gate.py`; `config/policies/publication_policy.yaml`; `services/release_orchestrator/runner.py`; `services/release_orchestrator/contract.py` | `tests/test_publication_pipeline_end_to_end.py` | enforced on one named docs-route publication path | `repo-proven` | The test sequences the real `compiled -> staged` curation step into the real release runner, then proves emitted manifest, constraints, and registry on named release surfaces and fail-closed cleanup when the final release registry append fails. |
| downstream lineage quality disclosure | `signal_agent/content/lineage_status.py`; `signal_agent/operator/routing_lineage_drilldown.py` | no focused test cited in this bundle | enforced as analysis output | `repo-supported` | These files explicitly disclose `exact`, `inferred`, `missing`, and legacy-catalog conditions. |
| publication and whitepaper file surfaces | `docs/publications/v0.1.0_deterministic_constraint_kernel_technical_note.md`; `docs/publications/v0.1.0_deterministic_constraint_kernel_framework_sheet.md`; `services/concept_formalization_spine/README.md`; `services/concept_formalization_spine/examples/sample_project/whitepaper.md`; `app/hq/exporter.py` | no test required for file existence | existing files only | `repo-supported` | These are publication surfaces, not enforcement proof. |

## Weak Or Missing Evidence

| concept | exact files | current gap |
|---|---|---|
| publication paths beyond the named docs-route proof | `app/hq/curation/curate.py`; `config/policies/publication_policy.yaml`; `services/release_orchestrator/runner.py`; `config/state_machine.yaml` | The bundle now proves one named `compiled -> staged -> emitted` path, but it does not yet prove every publication lane or emission surface end to end. |
| operator-style observed-write verification on publication and release surfaces | `app/hq/curation/curate.py`; `services/release_orchestrator/runner.py`; `app/utils/io_contract.py` | Curation and release now use stronger atomic persistence, but they still do not prove declared-vs-observed mutation verification like `signal_agent/operator/runtime.py`. |
| routing contract-resolution exception handling and source precedence | `shared/contract.py`; `app/hq/capture/router.py`; `tests/test_phase2_improvements.py` | Inference-only routing is now blocked, but `resolve_bundle_contract()` still uses broad exception fall-through before later sources are considered. |
